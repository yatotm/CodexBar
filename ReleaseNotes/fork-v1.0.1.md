移除 iCloud/CloudKit 功能，保留本地与 SSH/HTTPS 统计。

- 删除设置页云同步开关、菜单云状态、后台服务及 CloudKit 权限
- 本地 Hook 维护与重建继续可用，统计格式、去重规则和已保存数据保持不变
- 旧云缓存和偏好保留但不再使用，不向云端发送删除请求
- 睡眠相关功能保持上一版状态，未放开不满足签名条件的 Helper

安装方式：下载 DMG，将 `CodexBar Fork.app` 拖入 Applications。首次若被系统拦截，在 `系统设置 > 隐私与安全性` 中选择 `仍要打开`。此版本未经过 Apple 公证，更新包使用独立 Sparkle 签名。
