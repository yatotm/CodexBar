保留原有菜单栏交互，加入多机器实时任务，并启用授权后的防睡眠。

- 本机与 SSH 开发机的 Codex、Claude 任务合并到原任务卡片，按“模型、思考深度、机器”显示
- 展示工具调用、等待批准、上下文压缩和子智能体阶段；远端主动推送，不等待五分钟统计
- 合盖或睡眠时暂停连接，开盖联网后恢复；历史统计继续由远端定时采集
- 防睡眠统一为“关闭、手动、随任务”，首次开启需要允许后台运行；随任务跟随本机 Codex
- 菜单栏额度固定显示 7d，应用名称统一为 CodexBar
- 同一个安装包支持 Apple 芯片和 Intel，Apple 芯片原生运行

本版使用开发签名，未经过 Apple 公证。将 `CodexBar.app` 拖入 `/Applications`；首次打开如被系统拦截，在 `系统设置 > 隐私与安全性` 中选择 `仍要打开`。

旧安装若仍名为 `CodexBar Fork.app`，改名或移动前请退出应用，并先关闭旧 Hook，安装后重新开启。统计数据和偏好无需迁移。

已验证防睡眠的开启、关闭、退出恢复和同身份更新后的授权保留；实际合盖与 DarkWake 仍需结合设备验证。官网账本存在延迟，订阅切换当天的周期价值分摊仍有局限，金额仅供参考。
