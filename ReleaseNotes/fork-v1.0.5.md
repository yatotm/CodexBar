修复 Claude 后台子任务显示，适配新模型，并引入上游的流光外观设置和睡眠恢复改进。

- Claude 主任务回复后，仍在运行的后台子任务会继续显示在原任务卡片中；新一轮对话也会保留子任务数量、工具状态和等待批准信息
- 过滤 Claude 内部辅助任务的结束事件，避免覆盖真实任务状态；适配新版思考深度字段和 Opus 5.5 模型名称
- 补充 GPT-6 Sol、GPT-6 Luna 的价格与订阅 Fast 消耗系数，保留历史模型的估算口径
- 流光支持调整各状态颜色、动画速度、亮度和结束后的显示时长，统一覆盖本机与远端 Codex、Claude 任务；默认关闭，重连和修改时长不重放旧提醒
- 防睡眠关闭或任务结束时，恢复系统睡眠设置并交由 macOS 决定何时入睡，避免主动休眠造成意外黑屏或锁屏
- 修复设置侧面板首次操作下拉框时意外关闭的问题，安装包补齐 GPL 许可、原作者署名与源码入口

同一个安装包支持 Apple 芯片和 Intel，沿用开发签名，未经过 Apple 公证。首次安装将 `CodexBar.app` 拖入 `/Applications`；如被系统阻止，在“系统设置 > 隐私与安全性”中选择“仍要打开”。已有用户可通过应用内检查更新升级，防睡眠与自动重置沿用原有后台授权。

Codex 账户功能最低要求仍为 `0.145.0`，Hook 最低要求仍为 `0.150.0`。本次不需要重建历史统计或重新配置 Hook。
