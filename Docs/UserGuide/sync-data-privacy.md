# 数据、同步与隐私

简体中文 | [English](../en/UserGuide/sync-data-privacy.md)

## 两种跨设备统计

SSH/HTTPS 用量中心合并 Codex、Claude 的统计元数据；iCloud 仅同步本机 Codex Hook 的日级聚合。两者独立，不能用 iCloud 同步状态判断 VPS 是否更新。SSH、被动 Claude 额度和官网分析的细节见 [多机器用量中心](usage-center.md)

## 跨设备同步

在 `设置 > 高级` 开启 `跨设备同步`，可以合并同一 iCloud 账户下多台 Mac 的每日 Hook 统计。需要先开启 CodexBar Hook，登录 iCloud，并使用具备本 fork CloudKit 权限的签名构建。旧上游云端容器不与本 fork 共用。

首次开启会上传本机保留期内的统计，之后自动同步变化。关闭同步会保留本机数据。

| 状态 | 含义 |
| --- | --- |
| 同步未开启 | 同步开关或 CodexBar Hook 已关闭 |
| 正在同步 | 正在传输数据 |
| 已同步 | 最近一轮同步成功 |
| 同步失败 | 网络、iCloud 账户或服务暂时不可用，稍后重试 |

主面板底部和设置页显示同步状态，设置页还显示最近成功上传时间。

## 哪些数据会上传

同步数据保存在你的 iCloud 私有数据库中，包括日期、每日事件和会话统计、项目显示名、模型名，以及用于区分设备和避免重复计数的标识。

以下内容不参与同步：

- 原始 Hook 事件、会话和任务标识、完整工作目录
- 提问、Codex 回复、工具参数和输出
- Codex 账户、额度、Token 用量和留存重置
- App 设置、代理配置和密码
- 日志、实时任务状态和异常会话保护记录

**项目显示名会上传** 如果项目名包含不希望存入 iCloud 的信息，请关闭跨设备同步。

## 本机数据

| 数据 | 内容与保留方式 |
| --- | --- |
| Hook 记录与每日统计 | 时间、事件、模型、工具、项目和任务标识等，保留 210 天；每日统计中的会话和轮次明细仅保留最近 3 天 |
| 异常会话保护 | 无法直接还原的任务标识和时间，最长保留到最后进展后的 24 小时 |
| App 设置 | 保存在当前 Mac，包括代理配置；代理密码以明文保存 |
| 交互日志 | 最近 500 条 Codex 请求和响应，只在本次运行期间保留 |
| 用量中心 | SQLite 保存来源、经筛选的事件、Token 和额度记录；官网分析及周限历史按账号隔离缓存 |
| 后台服务状态 | 用于恢复睡眠设置和清理自动重置唤醒计划 |

CodexBar 读取本机 Codex 的任务状态和活动记录，但不保存提问、回复或工具内容，也不复制 Codex 登录凭据。

关闭代理会保留配置和密码；保存关闭身份验证的配置或选择 `删除配置` 时会删除密码。

## 重建数据

统计异常时，可在 `设置 > 高级 > 重建数据` 选择日期范围并确认。可选范围为最近 210 天，有本机记录的日期会显示标记。

重建使用保留的本机记录重新计算 Hook 统计，完成后显示处理结果。开启同步时，重建结果会替换当前设备对应日期的云端统计，其他设备的贡献保留。账户、额度和 Token 用量不受影响。

## 网络与日志

| 网络访问 | 用途 |
| --- | --- |
| Codex 服务 | 获取账户、额度和用量，执行自动重置 |
| 官方账号分析 | 复用本机 Codex OAuth，只读获取当前账号的官网统计；凭据不传给 VPS |
| SSH / HTTPS 来源 | 读取所配置设备的统计元数据；HTTPS 使用专用令牌 |
| 更新服务 | 从 `yatotm/CodexBar` 检查并下载更新 |
| iCloud | 开启同步后传输每日 Hook 统计 |

代理只用于 CodexBar 的 Codex 服务连接，不影响更新、iCloud 或其他 App。

交互日志可能包含账户和请求响应内容，退出 App 后清除。分享日志前，请检查其中是否有不希望公开的信息。

## Claude 与官网分析

Claude 采集只读取客户端已经留下的日志和缓存，不主动请求 Anthropic，不复制 OAuth 凭据。Codex 官网分析会读取本机登录文件中的 OAuth，并仅向 `chatgpt.com` 发出只读请求；不读取 Chrome Cookie，不自行续期或改写 Codex 凭据。

设备间只传输筛选后的统计元数据，包含项目显示名、模型、时间和哈希身份，不传输对话正文或工具参数。普通网页 Chat 对话不属于这里的 Codex 日志统计。美元结果是估算，不是账单。

返回 [用户指南](README.md)
